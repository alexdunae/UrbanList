<div id="cityBody">
	<% @lists.each do |list| %>
	<div id="cityBtn">
		<%= link_to list.name, city_list_description_path(city)%>
	</div>
	<%end%>

	<Br/>
	<%= link_to 'New List', new_city_list_path %>
</div>
